#pragma once

#include "glad/glad.h"

namespace goxel::rendering
{

enum class MeshHandle : GLuint
{
    INVALID = 0
};

enum class MaterialHandle : GLuint
{
    INVALID = 0
};

enum class TextureHandle : GLuint
{
    INVALID = 0
};

} // namespace goxel::rendering
